package gui;

/**
 * All possible cell state types in all automatons. Used by classes which behaviour depends on the type of the automaton.
 *
 * @see gui.StatePicker
 */
public enum AutomatonMode {
    BINARY, QUAD, WIREWORLD, ANT;
}
